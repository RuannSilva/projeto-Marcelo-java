/**
 * Camada model: entidades de domínio (ex: Produto, Usuario, Administrador)
 * e também as classes DAO (ex: ProdutoDAO), responsáveis pela persistência
 * dos dados (acesso direto ao BD, ou a uma lista em memória por enquanto).
 * Segundo o padrão MVC adotado neste curso, é o model quem fala com o BD —
 * não o controller.
 */
package com.marcelo.estoque.model;
