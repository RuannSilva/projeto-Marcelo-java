/**
 * Classes de modelo: representam os dados do domínio (ex: Produto, Usuario).
 * Só atributos, getters/setters e regras próprias do objeto — sem SQL, sem tela.
 */
package com.marcelo.estoque.model;
