/**
 * Classes de acesso a dados (DAO): sabem buscar, salvar, atualizar e remover
 * os models. Por enquanto podem guardar tudo em memória (ex: ArrayList);
 * depois viram acesso real ao banco (JDBC), sem mudar quem as usa.
 */
package com.marcelo.estoque.dao;
